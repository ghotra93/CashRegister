# Validation Report: 2026-09-21-cash-register-change-calculation

> Owner: `dotnet-validator` · Phase 6 · Generated by `.github/scripts/harness-dotnet.sh --report` + `harness-report-parsing`.
> Revision 2. It supersedes the run on `b135f84` (which predated T-018) so that the PASS covers the code being shipped.

## Summary

- **Verdict: PASS** ✅
- Run at: 2026-09-22T14:57:23Z → 14:59:04Z (fresh run; no cached results)
- Commit: `44f0b86` (clean working tree; all 18 tasks `done`; no open `Q-NNN`)
- Harness: `.github/scripts/harness-dotnet.sh` (deviations from the skill defaults: ADR-009)
- Regressions vs baseline: n/a, because there is no `.specs/_baseline.json` (greenfield; see F-001)
- Changes since revision 1: T-018 (host composition moved to `HostSetup`, review F-001) and the Docker files. Unit tests 149 → 159; branch coverage 99.28% → 100%; Gap-003 closed.

## Gates

| # | Layer | Tool | Result | Detail |
|---|---|---|---|---|
| 1 | Format | `dotnet format --verify-no-changes` | ✅ pass | pre-gate |
| 2 | Compile + analyzers + style | `dotnet build -c Release` (.NET analyzers `latest-recommended`, warnings as errors, style rules as errors) | ✅ pass | 0 warnings. Meziantou.Analyzer and XML docs are waived (ADR-009 #2) |
| 3 | Architecture | ArchUnitNET (inside the unit gate) | ✅ pass | 8 rules incl. slice cycles and boundaries, with positive controls |
| 4 | Unit | xUnit v3 / MTP, `--filter-not-trait Category=Integration` | ✅ pass | **159 / 159**, 0 skipped |
| 5 | Integration | xUnit v3 / MTP, `--filter-trait Category=Integration` | ✅ pass | **30 / 30**, 0 skipped (includes NFR-001 p95 < 500 ms at 1000 lines) |
| 6 | Coverage (unit run) | Microsoft.Testing.Extensions.CodeCoverage → merged Cobertura | ✅ pass | **line 100%, branch 100%** (floor 90%) |
| 7 | New-code coverage | `check-new-code-coverage-dotnet.sh` | ✅ pass | vs `origin/main`: **100% (27/27)**; vs feature base `9b945d3`: **100% (349/349)** |
| 8 | Mutation | Stryker.NET | ⏭ skipped | no `stryker-config.json`; mutation testing not opted in (expected, not a failure) |
| 9 | Contract | OpenAPI operation diff | ✅ pass | 0 breaking, 5 added (first publication; see F-002) |
| 10 | Security | `dotnet list package --vulnerable --include-transitive`, `npm audit` | ✅ pass | 0 critical / high / moderate / low in both |
| 11 | Web | ESLint, `tsc`, Vitest + v8 coverage, `vite build` | ✅ pass | 30 tests; line 100%, branch 90.38% (floor 90%) |
| 12 | Traceability | `traceability-dotnet.sh` | ✅ pass | 28/28 active ACs have tagged tests; 0 orphans |

## Coverage detail

| Scope | Line | Branch | Status |
|---|---|---|---|
| Overall .NET (unit run, first-party `src/`) | 100% | 100% | ✅ ≥ 95 |
| New code vs `origin/main` (T-013 onwards, incl. T-017 and T-018) | 100% (27/27) | — | ✅ ≥ 95 |
| New code, whole feature (vs `9b945d3`) | 100% (349/349) | — | ✅ ≥ 95 |
| Web (`web/src`) | 100% | 90.38% | ⚠️ 90–95 (above the floor) |

Per area: `src/CashRegister.Api` (host + `HostSetup`), the module root, `Features/Currencies` and every `Features/Change` sub-folder are all at **100% line and 100% branch**. The previously uncovered `?? []` CORS fallback is now `HostSetup.AllowedOrigins`, covered by `HostSetupTests`.

## Mutation detail

- **Skipped:** `stryker-config.json` is absent, so the gate is not configured. Candidates if opted in: `TransactionLineParser`, `ChangeCalculator`, `OwedDivisibleByRule`, `ChangeFormatter`.

## Contract diff

- Source: generated at runtime (`/openapi/v1.json`, Development) into `artifacts/openapi/openapi.json` (committed).
- Baseline: none yet on `origin/main` (first publication).
- Breaking: **none**. Added: `POST /api/files`, `GET /api/files`, `GET /api/files/{id}/output`, `GET /api/settings/divisor`, `PUT /api/settings/divisor`.
- The diff compares operations only (path + method), not schemas (ADR-009).

## Security findings

- NuGet (all projects, including transitive): **no vulnerable packages** (`artifacts/vulnerable.txt`). npm: 0 at every severity. Waivers: none. Build-time NuGet audit is on (`all`, level `high`).

## Findings

No blockers or majors.

- **F-001 (minor):** there is no `.specs/_baseline.json`, so no baseline diff was possible (greenfield). Remediation: create a baseline from this run's `artifacts/harness-summary.json`.
- **F-002 (minor):** the contract gate has no published baseline yet and diffs operations only. Remediation: once pushed, later runs diff against the published document.
- **F-003 (nit):** web branch coverage (90.38%) sits in the 90–95 band; the uncovered branches are React teardown guards (06-test-plan.md, Gap-007).
- **Accepted, with ADRs:** mutation not opted in; Meziantou.Analyzer and XML docs waived (ADR-009 #2); `HostCompositionTests` in the unit gate (ADR-009); Verify snapshot dropped (Gap-004).

## Baseline diff

- Baseline file `.specs/_baseline.json`: **absent** (greenfield). Entries added: 0. Removed: 0.

## Artifacts

- `artifacts/harness-summary.json` · `artifacts/harness.log`
- `artifacts/test-results/unit/*.trx`, `artifacts/test-results/it/*.trx`
- `artifacts/coverage/cobertura.xml` · `artifacts/coverage/unit/*.cobertura.xml` · `artifacts/coverage/web/cobertura-coverage.xml`
- `artifacts/new-code-coverage.json` (vs `origin/main`) · `artifacts/new-code-coverage.feature.json` (vs `9b945d3`)
- `artifacts/openapi/openapi.json` · `artifacts/vulnerable.txt` · `07a-traceability.md` (regenerated)

## Recommended next action

- `08-code-review.md` already records F-001 as resolved (verdict **Approve**). Continue with **`/net-ship`**, which still needs its two other pre-ship items: registering the phase-produced files in `04-tasks.md` (scope check), and a decision on the OpenTelemetry instrumentation rule (ADR-008).

## Sign-off

- [x] All required gates green, or skipped with a stated reason (mutation not configured).
- [x] No new baseline entries (no baseline file).
- [x] Every waiver has a covering ADR (ADR-009) or a test-plan decision (Gap-004).
