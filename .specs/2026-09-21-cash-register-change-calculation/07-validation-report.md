# Validation Report: 2026-09-21-cash-register-change-calculation

> Owner: `dotnet-validator` · Phase 6 · Generated by `.github/scripts/harness-dotnet.sh --report` + `harness-report-parsing`.

## Summary

- **Verdict: PASS** ✅
- Run at: 2026-09-21T21:51:37Z → 21:52:25Z (fresh run; no cached results)
- Commit: `b135f84` (clean working tree; all 17 tasks `done`; no open `Q-NNN`)
- Harness: `.github/scripts/harness-dotnet.sh` (wired in `d58d54c`; deviations from the skill defaults are in ADR-009)
- Regressions vs baseline: n/a, because there is no `.specs/_baseline.json` (greenfield; see F-001)
- Pre-existing baseline failures: none

## Gates

| # | Layer | Tool | Result | Detail |
|---|---|---|---|---|
| 1 | Format | `dotnet format --verify-no-changes` | ✅ pass | pre-gate |
| 2 | Compile + analyzers + style | `dotnet build -c Release` (.NET analyzers `latest-recommended`, warnings as errors, style rules enforced as errors) | ✅ pass | 0 warnings. Meziantou.Analyzer and XML docs are waived (ADR-009 #2) |
| 3 | Architecture | ArchUnitNET (inside the unit gate) | ✅ pass | 8 rules incl. slice cycles, boundaries, sealed rules, no system clock; positive controls pass |
| 4 | Unit | xUnit v3 / MTP, `--filter-not-trait Category=Integration` | ✅ pass | **149 / 149**, 0 skipped |
| 5 | Integration | xUnit v3 / MTP, `--filter-trait Category=Integration` | ✅ pass | **30 / 30**, 0 skipped (includes NFR-001 p95 < 500 ms at 1000 lines) |
| 6 | Coverage (unit run) | Microsoft.Testing.Extensions.CodeCoverage → merged Cobertura | ✅ pass | **line 100%, branch 99.28%** (floor 90%) |
| 7 | New-code coverage | `check-new-code-coverage-dotnet.sh` | ✅ pass | vs `origin/main`: 0 executable lines changed (see F-003); vs feature base `9b945d3`: **100% (343/343)** |
| 8 | Mutation | Stryker.NET | ⏭ skipped | no `stryker-config.json`; mutation testing not opted in (expected, not a failure) |
| 9 | Contract | OpenAPI operation diff | ✅ pass | 0 breaking, 5 added (first publication; see F-002) |
| 10 | Security | `dotnet list package --vulnerable --include-transitive`, `npm audit` | ✅ pass | 0 critical / high / moderate / low in both |
| 11 | Web | ESLint, `tsc`, Vitest + v8 coverage, `vite build` | ✅ pass | lint, typecheck and build clean; 30 tests; line 100%, branch 90.38% (floor 90%) |
| 12 | Traceability | `traceability-dotnet.sh` | ✅ pass | 28/28 active ACs have tagged tests; 0 orphans |

## Coverage detail

### Overall (unit run, first-party `src/` only)

| Scope | Line | Branch | Status |
|---|---|---|---|
| Overall .NET | 100% | 99.28% | ✅ ≥ 95 |
| New code vs `origin/main` | n/a (0 executable lines changed) | — | ✅ |
| New code, whole feature (vs `9b945d3`) | 100% (343/343) | — | ✅ ≥ 95 |
| Web (`web/src`) | 100% | 90.38% | ⚠️ 90–95 (above the floor) |

### Per project / slice

| Area | Line | Branch |
|---|---|---|
| `src/CashRegister.Api` (host) | 100% (24/24) | 92.9% (13/14): the uncovered branch is `Program.cs:21` `?? []`, Gap-003 won't fix |
| `src/CashRegister` (module root: registration, options) | 100% | 100% |
| `Features/Currencies` | 100% | 100% |
| `Features/Change/Money` · `Strategies` · `Rules` · `ChangeCalculator` | 100% | 100% |
| `Features/Change/Parsing` · `Formatting` · `Processing` | 100% | 100% |
| `Features/Change/Files` · `Settings` (endpoints) | 100% | 100% |

Coverage source is the unit run only (canonical; ADR-009 #1). The host's startup code is included through `HostCompositionTests`, by user decision in T-017.

## Mutation detail

- **Skipped:** `stryker-config.json` is absent, so the gate is not configured. Candidates if opted in: `TransactionLineParser`, `ChangeCalculator`, `OwedDivisibleByRule`, `ChangeFormatter` (06-test-plan.md).

## Contract diff

- Source: generated at runtime from the Minimal API endpoints (`/openapi/v1.json`, Development), written to `artifacts/openapi/openapi.json` (committed).
- Baseline: none. `origin/main` has no `artifacts/openapi/openapi.json` yet, so this is the first publication.
- Breaking changes: **none**.
- Non-breaking (added): `POST /api/files`, `GET /api/files`, `GET /api/files/{id}/output`, `GET /api/settings/divisor`, `PUT /api/settings/divisor`.
- Scope: added and removed operations (path + method) only; schema-level changes are not diffed (ADR-009).

## Security findings

- NuGet (all projects, including transitive): **no vulnerable packages** (`artifacts/vulnerable.txt`).
- npm (`web/`): 0 critical, 0 high, 0 moderate, 0 low.
- Waivers: none. Build-time NuGet audit is also on (`NuGetAuditMode=all`, level `high`).

## Findings

No blockers or majors.

- **F-001 (minor):** there is no `.specs/_baseline.json`, so no baseline diff was possible. This is expected for a greenfield feature. Remediation: create a baseline from this run's `artifacts/harness-summary.json` so future runs can report regressions.
- **F-002 (minor):** the contract gate has no baseline yet because `origin/main` predates `artifacts/openapi/openapi.json`, and it diffs operations only, not schemas. Remediation: once this branch is pushed, later runs diff against the published document. A schema-level diff would need a new tool (ADR-009).
- **F-003 (nit):** new-code coverage against the default `BASE_REF=origin/main` measures 0 lines, because `origin/main` already contains T-001 to T-012 and T-013 onwards changed only signatures, tests, web code and docs. Measured against the feature base (`BASE_REF=9b945d3`) it is **100% (343/343)**. Informational only.
- **F-004 (nit):** web branch coverage is 90.38%, inside the 90–95 ⚠️ band. The 5 uncovered branches are React teardown guards (06-test-plan.md, Gap-007).
- **Accepted, with ADRs:** mutation not opted in; Meziantou.Analyzer and XML docs waived (ADR-009 #2); `Program.cs:21` fallback branch won't fix (Gap-003); Verify snapshot dropped by user decision (Gap-004).

## Baseline diff

- Baseline file: `.specs/_baseline.json`, **absent** (greenfield). Entries added: 0. Entries removed: 0.

## Artifacts

- `artifacts/harness-summary.json` (gate summary) · `artifacts/harness.log`
- `artifacts/test-results/unit/*.trx`, `artifacts/test-results/it/*.trx`
- `artifacts/coverage/cobertura.xml` (merged unit) · `artifacts/coverage/unit/*.cobertura.xml` · `artifacts/coverage/web/cobertura-coverage.xml`
- `artifacts/new-code-coverage.json` (vs `origin/main`) · `artifacts/new-code-coverage.feature.json` (vs `9b945d3`)
- `artifacts/openapi/openapi.json` · `artifacts/vulnerable.txt`
- `07a-traceability.md` (regenerated this run)

## Recommended next action

- **`/net-review`**, the pre-commit review of the full diff.
- Optional: create `.specs/_baseline.json` from this run (F-001).

## Sign-off

- [x] All required gates green, or skipped with a stated reason (mutation not configured).
- [x] No new baseline entries (no baseline file).
- [x] Every waiver has a covering ADR (ADR-009) or a test-plan decision (Gap-003, Gap-004).
