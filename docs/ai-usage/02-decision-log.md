# 2. Decision log: where AI output was used, modified or rejected

This log is built from the full session transcript ([01-transcript.md](01-transcript.md)) and the git history. Each entry cites where it can be checked. "AI" means Claude (Claude Code, model Opus 5). "Developer" means Harpreet Ghotra.

> **[Author: review this log.]** It records every decision visible in the transcript. Add any edits you made outside the chat (for example, by hand in the IDE) that the transcript cannot show.

## How to read the rest of the repository

- **All source code, tests, configuration, Dockerfiles and documents in this pull request were generated by the AI** under the developer's direction, then reviewed and committed by the developer (every commit is the developer's).
- The developer's own contributions are the **requirements decisions** (answers to 20 open questions in the spec), **approvals and rejections** at each decision point, **package approvals**, **commit messages**, and **sign-offs**. The entries below list them.
- Files the developer edited directly (seen in the transcript as "changed on disk" by someone other than the AI):
  - `.specs/…/01-spec.md` (answers to Q-001 to Q-015; sign-off ticks);
  - `.specs/…/02-spec-review.md` (answers to NQ-001 to NQ-005);
  - `.specs/…/03-design.md` and `04-tasks.md` (sign-off ticks);
  - `.specs/…/09-ship-plan.md` (release-notes sign-off);
  - deletion of the AI's first-pass scaffold (`src/`, `tests/`, the `.sln`) before T-001.

## A. Rejected by the developer

| # | What the AI produced or proposed | Developer's decision | Reason given |
|---|---|---|---|
| R-1 | After reading the README, the AI planned a solution and **started writing domain code straight away** (first-pass layered projects `CashRegister.Domain` / `.Application`). | Stopped the tool call and redirected to a **spec-first workflow** (`/net-spec`, then review, plan and TDD build). Later deleted the first-pass scaffold. | **[Author: state the reason.]** The transcript shows the interruption and the switch to `/net-spec`, but no stated reason. |
| R-2 | Harness coverage: the AI **recommended** measuring coverage on unit + integration runs merged (100% at the time). | Chose the canonical **unit-only** coverage gate, and asked for more unit tests instead (which became T-017). | Chosen from the options offered; follows the harness skill's rule rather than bending it. |
| R-3 | For `Program.cs` coverage, the AI **recommended** excluding the host from the unit gate. | Chose **"add a unit-gate host test"** instead (`HostCompositionTests`). | Chosen from the options offered. |
| R-4 | The harness skill's `.editorconfig` default: private fields `camelCase` without an underscore. | **Rejected**, with the developer's own rule: *"c# is PascalCase, private fields can use _camelCase no hungarian notation"*. | Stated by the developer; now enforced as build errors (ADR-009). |
| R-5 | The harness skill's `Meziantou.Analyzer` + XML documentation (about 36 findings measured). | **Skipped** both. | Chosen from the options offered (ADR-009). |
| R-6 | `Verify.XunitV3` OpenAPI snapshot: first approved by the developer, then the AI found the package fails the build unless the project declares compliance with Verify's maintenance-fee licence. | **Dropped** the snapshot (Gap-004); a path/method smoke test stays. | Licence requirement; the AI deliberately did not choose a licence option for the developer. |
| R-7 | Ship plan: the AI **recommended** alerts on `/health` and on the NFR-001 time budget. | Chose **no alerts** (demo, no on-call). | Chosen from the options offered (ADR-008 amendment). |
| R-8 | The AI proposed multi-line commit messages. | Asked for **single-line** commit messages and used those. | Developer preference. |

## B. Modified by the developer (AI proposal changed or completed)

| # | AI output | Modification | Reason |
|---|---|---|---|
| M-1 | Spec draft with 15 open questions (Q-001 to Q-015). | The developer **answered every question in the file**, which set core behaviour: divisibility in cents, `No change` output, per-line errors, 1000-line limit, a UI-changeable divisor, downloads, USD-only with an open/closed design, no auth in v1. | Requirements are the developer's to decide; the AI was instructed never to invent answers. |
| M-2 | Spec review found 5 follow-up questions, including two answers that listed both options. | The developer answered NQ-001 to NQ-005 (in-memory state, divisor ≥ 1, one module with folders, OpenTelemetry in v2). | Resolve ambiguity before design. |
| M-3 | The AI's multiple-choice options at decision points. | The developer sometimes wrote their own answer rather than picking an option (R-4 is the clearest example). | See R-4. |
| M-4 | The AI's code review requested changes (F-001 major: logic in `Program.cs`). | The developer chose **"Fix it"**, which became T-018. The 6 minor and 3 nit findings were left as recommendations. | Close the blocking finding; keep the minors for later. |
| M-5 | `/net-ship` blocked on missing OpenTelemetry instrumentation. | The developer **waived** the rule for v1 (ADR-008 amendment). | Consistent with the earlier decision to defer OpenTelemetry to v2. |
| M-6 | The AI's draft external release notes (ship plan section 7). | The developer reviewed them and ticked the "edited by a human" sign-off. | Release-note sign-off is a human step. |
| M-7 | Package additions proposed by the AI. | Approved: the plan's NuGet test packages; `@vitest/coverage-v8`; the fix to the Windows path bug in the `.claude/hooks` scripts. Declined: `FakeTimeProvider` (not approved, so the tests use a hand-written fake clock). | New dependencies need the developer's approval. |

## C. Used directly (AI output accepted as proposed)

- **Specification, design and all nine ADRs** (after M-1/M-2 answers): module layout, integer-cent money, rules → strategies with priorities, currency registry, ProblemDetails error model, output and error wording (ADR-007).
- **Implementation of all 18 tasks** (T-001 to T-018), tests included; see the commits in [04-tools-and-tasks.md](04-tools-and-tasks.md).
- **Vite + React SPA instead of Next.js** (ADR-006): the AI's recommendation, accepted.
- **Quality harness scripts** (`.github/scripts/**`), the **Docker / docker-compose** setup, the **README Solution section** and the **pull request description**.

## D. AI output the AI itself rejected or corrected (caught by verification)

These are places where the AI's first attempt was wrong and was fixed before commit. They are listed so the reviewer can judge how much the verification process caught.

| # | Where | What was wrong | How it was caught | Fix |
|---|---|---|---|---|
| D-1 | T-001 | The first smoke test could never pass: the compiler drops references to assemblies whose types are unused. | Red/green reasoning before implementing. | Load the assembly by name. |
| D-2 | `.claude/hooks` | The scope-enforcement hook blocked every in-scope test file on Windows (backslash paths). | Hook blocked the red step. | Normalise `\` → `/` (developer approved). |
| D-3 | T-005 | A test couldn't tell the two change methods apart ("1 dollar" also has count 1). | Test failed after implementation. | Assert on the denomination instead. |
| D-4 | T-011 | **Real bug:** a non-integer divisor returned **500**, not 400. | Integration test. | Exception-handler status selector (later moved into `HostSetup`). |
| D-5 | T-011 | A test read the HTTP body twice. | Test error. | Helper returns the parsed body. |
| D-6 | T-012 | **An architecture rule was vacuous**: ASP.NET types weren't loaded, so "only endpoints use HTTP types" could never fail. | Positive-control test failed. | Load the ASP.NET assemblies; fix the control. |
| D-7 | T-013 | Multipart uploads failed under jsdom (`FormData`/`File` from jsdom, `fetch` from Node). | Tests failed; the AI probed which globals came from where. | Switched to happy-dom (deviation recorded). |
| D-8 | T-014 | **Real bug:** `min={1}` on the input stopped `0` being submitted, so the server's "Invalid divisor" message was never shown. | Component test. | Removed `min`; the server is the single validator. |
| D-9 | T-010 | `HttpClient` refuses whitespace-only file names, so the planned test couldn't run. | Test error. | Use `folder/` to reach the same fallback. |
| D-10 | Harness | A solution-wide coverage run let one test process overwrite another's report. | Probe of the output files. | Per-project runs + merge script. |
| D-11 | T-017 | A test named "without a CORS section" never reached that path. | Coverage showed the branch still uncovered. | Test removed (misleading). |
| D-12 | Code review | 10 findings on the AI's own code, including F-001 (logic in `Program.cs`, fixed in T-018) and F-002 (a 1 MB+ upload shows a generic "Bad Request", confirmed live, not yet fixed). | Rubric-based review of `9b945d3...HEAD`. | See `.specs/…/08-code-review.md`. |

Every red failure, test bug, real bug and plan change above is also logged per task in `.specs/2026-09-21-cash-register-change-calculation/05-implementation-log.md`.
